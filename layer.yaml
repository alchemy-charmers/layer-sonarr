includes: ['layer:basic','layer:version','layer:hostname','layer:backup','interface:usenet-downloader','interface:plex-info', 'interface:reverseproxy']  # if you use any interfaces, add them here
repo: git@github.com:chris-sanders/layer-sonarr.git
options:
  backup:
    backup-method: "sync"
    backup-files: >-
      /home/{sonarr-user}/.config/NzbDrone/Backups/scheduled

